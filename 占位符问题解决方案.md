# 占位符实体问题解决方案

## 问题原因

占位符实体是在**关系数据同步**时产生的。当系统发现关系中引用的某些实体ID在云端不存在时，为了保持关系数据的完整性，就会创建占位符实体。

这种情况通常发生在：
1. 实体被删除但关系数据仍然引用它
2. 不同设备间数据同步不一致
3. 从消息中提取的实体关系引用了不存在的实体

## 解决方案

### 1. 清理现有占位符（立即执行）

使用提供的 `cleanup-placeholders.js` 工具：

```bash
# 在浏览器控制台中执行
const cleaner = new PlaceholderCleaner();
await cleaner.initialize();

# 先预览
await cleaner.previewPlaceholders();

# 确认清理
cleaner.forceClean = true;
await cleaner.cleanPlaceholders();
```

### 2. 防止新占位符产生（代码改进）

已修改 `CacheStrategy.ts` 和 `LocalCache.ts`：

#### 改进点：
- **不再在云端创建占位符** - 保持云端数据干净
- **仅在本地缓存创建占位符** - 用于维护关系完整性
- **改进关系查询逻辑** - 优雅处理缺失实体引用
- **增加详细日志** - 记录缺失实体信息

#### 技术细节：
1. `createPlaceholderEntity()` → `createLocalPlaceholder()` 
   - 只调用 `localCache.cacheEntity()` 
   - 不调用 `cloudStorage.storeEntity()`

2. `traverseRelationships()` 改进：
   - 验证关系两端实体是否存在
   - 跳过无效关系和不存在的实体
   - 只遍历存在的相关实体

## 优势

### ✅ 解决的问题：
- 云端不再有垃圾占位符数据
- 关系查询更加健壮
- 数据同步更加干净
- 减少云端存储空间占用

### ✅ 保持的功能：
- 关系网络依然完整
- 本地查询不会出错
- 数据同步正常工作
- 用户体验无影响

## 使用建议

1. **立即清理现有占位符**：使用清理工具删除云端占位符
2. **部署代码改进**：使用修改后的代码防止新占位符
3. **监控日志**：关注缺失实体的警告日志
4. **定期检查**：偶尔查看是否有新的数据不一致问题

## 注意事项

- 清理占位符不可撤销，建议先备份
- 本地占位符是临时的，不会同步到云端
- 如果发现大量缺失实体，可能需要检查数据同步逻辑
- 清理工具使用完毕后请删除

## 监控指标

可以通过以下日志监控系统健康状态：

```
⚠️  发现 X 个缺失实体，已在本地缓存中创建占位符
关系遍历中发现缺失实体: entity_id
跳过不存在的相关实体: entity_id
跳过无效关系 (两端实体都不存在): relationship_id
```

如果这些警告频繁出现，说明可能存在数据同步问题需要进一步调查。
