# App Script 自动更新机制

## 概述

为了让用户能够在扩展更新时自动获取最新的 App Script 代码，我们实现了一套完整的自动更新机制。该机制使用 Google Apps Script API 的 `deployments.update` 接口，确保在更新代码的同时保持 Web App URL 不变。

当前实现会在后台静默检查、在管理页面提供手动检查和升级入口，并在升级前展示当前/最新版本、Project History 版本额度、URL 不变和失败回退旧 deployment 的说明。升级会先读取扩展内置模板版本；如果模板版本不可读或不是合法 SemVer，会直接停止检查/升级并在管理页显示可重试提示，不会用默认版本兜底。随后会用不携带 Chrome profile cookie 的匿名请求重新读取线上 Web App 版本，已是最新或更高版本时直接跳过脚本写入和版本创建；确实需要升级时，才预检是否存在可更新的正式 Web App deployment，并要求该 deployment 的 Web App URL 匹配当前配置 URL，再检查 Apps Script Project History 是否还有新建版本额度，然后写入代码、创建新版本、更新 deployment，并再次调用当前 Web App URL 的版本端点确认新版本已经生效。只有确认 Web App URL 已返回目标版本后，才会把 Sheet/Storage 配置标记为最新，避免 API 提交成功但外部入口仍服务旧脚本时出现假成功；如果 deployment 已提交但版本端点始终无法确认目标版本，会尝试把 deployment 指回升级前的版本。版本探测临时失败或非 JSON 响应不会被当成旧版脚本，旧版兼容只用于 Web App 正常返回 JSON 但缺少 `version` 字段的情况；如果检测到需要升级但配置缺少 Script ID，会停止展示可升级入口并提示重新绑定。版本比较按 SemVer 优先级处理，兼容常见的 `v` 前缀和 build metadata，避免因为 `2.6.16+build.7` 这类等价版本造成重复升级。

## 功能特性

### 1. 版本标记

在 `app-script-template.gs` 中添加了版本常量：

```javascript
// App Script 版本号（用于检测更新）
var APP_SCRIPT_VERSION = '2.8.4';
var APP_SCRIPT_LAST_UPDATED = '2026-05-19';
```

同时提供了一个 API 端点来获取当前部署的版本：

```javascript
// 获取 App Script 版本信息
if (action === 'getVersion') {
  return ContentService.createTextOutput(
    JSON.stringify({ 
      version: APP_SCRIPT_VERSION,
      lastUpdated: APP_SCRIPT_LAST_UPDATED
    })
  ).setMimeType(ContentService.MimeType.JSON);
}
```

### 2. 配置扩展

在 `SheetConfig` 接口中添加了版本相关字段：

```typescript
export interface SheetConfig {
  // ... 其他字段
  deploymentId?: string;         // Web App deployment ID（用于更新部署）
  appScriptVersion?: string;     // App Script 版本号
  appScriptLastUpdated?: string; // App Script 最后更新时间
}
```

### 3. 自动更新器

创建了 `AppScriptUpdater` 类，提供以下功能：

- **检查更新**: `checkForUpdates()` - 对比本地版本和部署版本
- **执行更新**: `updateAppScript()` - 先重新确认线上版本，再预检正式 deployment，最后自动更新 App Script 代码、重新部署并确认当前 URL 已生效
- **模板版本保护**: 如果扩展包里的 `app-script-template.gs` 无法读取版本号，或版本号不是合法 SemVer，会停止检查/升级并提示重试，不会把失败缓存成默认版本
- **幂等跳过**: 如果线上 Web App 已经是最新或更高版本，直接返回成功，不写入项目代码，不创建新的 Project History 版本
- **探测失败保护**: 如果升级前无法确认线上版本，会停止升级并提示重试，避免网络/API 临时失败时消耗脚本版本额度
- **版本端点匿名探测**: 读取 `getVersion` 时显式不携带 Chrome profile cookie，避免 Google 多账号登录态把公开 Web App 重定向到错误的 `/u/N/` 账号上下文后返回 404
- **版本端点 URL 保护**: 读取 `getVersion` 时会保留 Web App URL 里已有的查询参数，再追加 `action=getVersion`，避免旧绑定或调试参数把版本探测拼成无效地址
- **deployment URL 匹配保护**: 查找正式 deployment 时会比对 `entryPoints.webApp.url` 和当前配置的 Web App URL；如果存在多个 deployment 但没有 URL 匹配项，会停止升级并提示用户重新绑定或检查 Manage deployments，并提供 Apps Script 项目入口
- **版本额度预检**: 升级前通过 `projects.versions.list` 读取 Project History 使用量；达到 200 个版本上限时直接提示清理，不先覆盖脚本代码
- **部署生效确认**: `deployments.update` 返回成功后会轮询当前 Web App URL 的 `getVersion` 端点；未确认返回目标版本时，不会把配置标记为最新
- **失败回退旧 deployment**: 如果 deployment 已更新但版本端点始终无法确认目标版本，会尝试把 deployment 回退到升级前记录的 versionNumber，并在错误提示中说明回退结果
- **版本比较**: 使用 `compareAppScriptVersions()` 按 SemVer 优先级比较版本，build metadata 不参与排序，异常旧值会被视为需要恢复升级
- **版本解析**: 从 `app-script-template.gs` 读取最新版本和更新时间，避免在多个文件重复维护版本号

### 4. 自动检测机制

#### 在 Background Script 中

扩展更新时自动触发检查：

```typescript
// 如果是扩展更新，检查并更新 App Script
if (details.reason === 'update') {
  console.log('🔄 检测到扩展更新，检查 App Script 是否需要更新...');
  checkAndUpdateAppScript().catch(error => {
    console.error('❌ App Script 自动更新失败:', error);
  });
}
```

#### 在 UI 中

用户打开定时消息管理页面时会静默检查更新：只复用 Chrome 已缓存的 Google 授权，不会在页面加载时弹出授权窗口。如果没有缓存授权，会显示一个非阻塞提示，用户可以手动点击“检查脚本”再授权检查。

如果发现有新版本，header 和页面提示条会显示“升级调度系统”入口；用户手动点击升级时才会触发交互式授权，并依次检查 Sheet Schema、App Script 和 Jira Automation 规则。提示条会说明当前版本、最新版本、Project History 额度、上次检查时间、升级前会重新确认线上版本、预检 deployment 和 Web App URL 匹配、检查 Project History 额度、更新后同步 Sheet 配置，Web App URL 会保持不变，失败项会保留旧版本。如果 Project History 接近或达到上限，会提前提示清理旧版本；已满时会把主操作切换为打开清理页面。若检查失败或无法获取授权，管理页会清掉旧的可升级入口，避免用户按过期状态继续升级。

## 更新流程

### 自动更新流程

1. **扩展更新触发**
   - 用户更新 Personal AI 扩展到新版本
   - `chrome.runtime.onInstalled` 监听器检测到 `update` 事件

2. **版本检查**
   - 延迟 3 秒后开始检查（避免与其他初始化冲突）
   - 从 Chrome Storage 读取配置
   - 只使用缓存授权；没有缓存 token 时跳过后台检查，不打断用户
   - 调用 Web App 的 `getVersion` 端点获取当前部署版本
   - 如果版本探测失败，跳过本次自动升级；如果线上已是最新，会同步配置缓存
   - 与扩展中的最新版本对比

3. **执行更新**（如果需要）
   - 重新读取 Web App 当前版本；如果已经是最新或更高版本，跳过 App Script 写入和版本创建
   - 如果重新读取失败，停止升级，不继续写代码或创建版本
   - 先查找并验证有 `versionNumber` 的正式 Web App deployment，并比对其 Web App URL 匹配当前配置；如果只有 @HEAD、没有 Web App deployment，或多个正式 deployment 都不匹配当前 URL，升级会在创建脚本版本前失败
   - 读取 Project History 版本数量；如果已达到 200 个版本上限，直接提示用户清理，不写入 HEAD 代码
   - 加载最新的 App Script 模板代码
   - 使用 `projects/{scriptId}/content` API 更新项目代码
   - 创建新版本 `projects/{scriptId}/versions`
   - 使用 `projects/{scriptId}/deployments/{deploymentId}` API **更新**现有部署
   - 再次调用当前 Web App URL 的 `getVersion` 端点，确认该 URL 已返回新版本
   - 如果提交后版本端点没有确认目标版本，尝试把 deployment 回退到升级前的 versionNumber
   - 更新配置中的版本信息

4. **通知用户**
   - 成功：显示系统通知告知更新完成
   - 失败：显示错误通知，提示手动更新

### 手动更新流程

用户也可以在 UI 中手动触发更新：

1. 打开定时消息管理页面
2. 如果有新版本，会在 header 显示“升级调度系统”按钮
3. 点击按钮确认更新
4. 等待更新完成

## 核心 API 使用

### 使用 `deployments.update` 而非 `create`

**为什么使用 `update`？**

使用 `projects.deployments.update` API 更新现有部署可以：
- ✅ 保持 Web App URL 不变
- ✅ 无需用户重新配置 Jira Automation
- ✅ 无需更新 Chrome Storage 中的配置
- ✅ 用户无感知升级

**API 调用示例：**

```typescript
await fetch(
  `https://script.googleapis.com/v1/projects/${scriptId}/deployments/${deploymentId}`,
  {
    method: 'PUT',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      deploymentConfig: {
        versionNumber: versionNumber,
        manifestFileName: 'appsscript',
        description: `Personal AI Scheduled Messages v${version}`
      }
    })
  }
);
```

## 版本号管理

### 更新版本号步骤

当需要发布新版本的 App Script 时：

1. **更新模板文件版本**
   
   编辑 `src/scheduled-messages/app-script-template.gs`:
   
   ```javascript
   var APP_SCRIPT_VERSION = '1.1.0';  // 更新版本号
   var APP_SCRIPT_LAST_UPDATED = '2025-12-15';  // 更新日期
   ```

2. **构建并发布扩展**
   
   版本号会随模板文件进入扩展包。初始化新系统、检查更新和升级成功后的配置同步都会读取该模板版本。

### 版本号规范

使用语义化版本号（Semantic Versioning）：

- **主版本号 (Major)**: 不兼容的 API 修改
- **次版本号 (Minor)**: 向下兼容的功能性新增
- **修订号 (Patch)**: 向下兼容的问题修正

示例：`1.2.3` → `主.次.修订`

## 配置持久化

更新成功后，配置会同步到两个位置：

1. **Chrome Storage** (`chrome.storage.local`)
   - 快速读取
   - 本地缓存

2. **Google Sheet** (Config 表)
   - 持久化存储
   - 跨设备同步
   - 便于查看和调试
   - 保存 `deployment_id`、`app_script_version`、`app_script_last_updated`

## 错误处理

### 常见错误场景

1. **未找到配置**
   - 跳过更新检查
   - 仅在已初始化的系统中运行

2. **无法获取 Google 授权**
   - 跳过更新
   - 等待下次检查

3. **Deployment ID 不存在或 URL 不匹配**
   - 自动从 API 获取现有 deployment 列表
   - 找到有 `versionNumber` 且 Web App URL 匹配当前配置的正式 Web App deployment
   - 保存到配置供后续使用
   - 如果有正式 deployment 但 URL 都不匹配，会停止升级，避免更新错误入口

4. **更新失败**
   - 显示错误通知
   - 保留当前版本
   - 用户可稍后重试

5. **App Script 历史版本达到 200 个上限**
   - 识别 Google 返回的版本上限错误
   - 升级前主动读取 Project History 用量；已满时直接阻止升级写入
   - 提供 Project History 清理入口
   - 用户清理旧版本后可重试升级

6. **版本探测临时失败**
   - 网络错误或 Web App 非 2xx 响应会停止检查/升级
   - HTML 登录页、权限页、错误页等非 JSON 响应不会按旧版脚本继续升级
   - 只有 Web App 正常返回 JSON 但缺少 `version` 字段时，才按旧版脚本继续升级
   - 避免因为短暂失败重复创建 Apps Script Project History 版本

7. **内置模板版本读取失败**
   - 扩展包无法读取 `APP_SCRIPT_VERSION` 或 `APP_SCRIPT_LAST_UPDATED` 时，检查结果会显示失败
   - `APP_SCRIPT_VERSION` 不是合法 SemVer 时同样停止检查/升级
   - 不会把失败兜底为 `1.0.0`，也不会缓存错误结果
   - 管理页展示可重试提示，当前线上脚本不会被自动改动

8. **部署已提交但版本未确认生效**
   - `deployments.update` 成功后，如果当前 Web App URL 仍返回旧版本或版本端点不可确认，会提示稍后重试检查
   - 此时不会把 Sheet/Storage 配置标记为最新，避免下一次检查误判为已升级
   - 系统会尝试把 deployment 回退到升级前的版本；如果回退失败，会在错误中展示回退失败原因和 Apps Script 项目入口
   - 管理页会说明可打开 Apps Script 项目检查 Manage deployments 是否指向新版本

### 已知问题和解决方案

#### ❌ Read-only deployments may not be modified

**问题描述：**
尝试更新 deployment 时遇到错误：
```json
{
  "error": {
    "code": 400,
    "message": "Read-only deployments may not be modified.",
    "status": "INVALID_ARGUMENT"
  }
}
```

**原因：**
Google Apps Script 有两种 deployment 类型：

1. **@HEAD Deployment（测试部署）**
   ```json
   {
     "deploymentId": "AKfycbw...",
     "deploymentConfig": {
       "scriptId": "...",
       "manifestFileName": "appsscript"
       // ❌ 没有 versionNumber
     },
     "updateTime": "1970-01-01T00:00:00Z"  // 特殊标记
   }
   ```
   - ❌ **只读**：不能通过 API 修改
   - ✨ 自动使用最新代码
   - 👻 在 UI 中可能不可见

2. **正式版本 Deployment**
   ```json
   {
     "deploymentId": "AKfycby...",
     "deploymentConfig": {
       "scriptId": "...",
       "versionNumber": 1,  // ✅ 有 versionNumber
       "manifestFileName": "appsscript",
       "description": "..."
     },
     "updateTime": "2025-12-01T07:22:08.111Z"
   }
   ```
   - ✅ **可以更新**：通过 `deployments.update` API
   - 📌 锁定到特定版本
   - 👀 在 UI 中可见

**解决方案：**

✅ **过滤掉 @HEAD deployment**，只使用有 `versionNumber` 且 Web App URL 匹配当前配置的正式 deployment：

```typescript
const versionedDeployments = deployments.filter(isVersionedWebAppDeployment);
const versionedDeployment = versionedDeployments.find((deployment: any) =>
  deploymentMatchesConfiguredWebAppUrl(deployment)
);
```

**实现细节：**

1. **优先使用配置中的 deploymentId**
   - 初始化时保存的 deploymentId（有 versionNumber）
   - 验证其是否仍然有效

2. **验证 deployment 类型**
   - 检查是否有 `versionNumber` 字段
   - 检查 `entryPoints.webApp.url` 是否匹配当前 Web App URL
   - 只有正式版本才能更新

3. **兼容老版本**
   - 如果配置中的 deploymentId 是 @HEAD，自动查找正式版本
   - 如果找到 URL 匹配的正式版本，更新配置保存正确的 deploymentId
   - 如果找不到 URL 匹配项，停止升级并提示用户重新绑定当前 Web App URL

**影响的文件：**
- `src/scheduled-messages/AppScriptUpdater.ts` - 过滤和验证 deployment

---

#### ❌ document is not defined

**问题描述：**
在 background service worker 中使用动态 `import()` 时，webpack 会尝试访问 `document` 对象，导致错误：
```
ReferenceError: document is not defined
    at __webpack_require__.l (background.js:41600:28)
```

**原因：**
- Chrome Extension 的 background script 运行在 service worker 环境中
- Service worker 没有 DOM，因此没有 `document` 对象
- Webpack 的动态导入机制在某些情况下会尝试使用 `document.createElement('script')` 来加载模块

**解决方案：**
✅ 使用**静态导入**而非动态导入：

```typescript
// ❌ 错误的做法（会导致 document is not defined）
const { AppScriptUpdater } = await import('./scheduled-messages/AppScriptUpdater');

// ✅ 正确的做法
import { AppScriptUpdater } from './scheduled-messages/AppScriptUpdater';
```

**影响的文件：**
- `src/background.ts` - 使用静态导入 `AppScriptUpdater`
- `src/scheduled-messages/ScheduledMessagesManager.tsx` - 使用静态导入 `AppScriptUpdater`
- `src/scheduled-messages/AppScriptUpdater.ts` - 使用静态导入 `ConfigSyncService`

### 降级策略

如果更新失败，系统会保持在旧版本，不会影响现有功能。用户可以：
- 等待下次自动检查
- 在 UI 中手动重试
- 联系管理员支持

## 测试更新机制

### 模拟扩展更新

1. 修改 `app-script-template.gs` 中的版本号
2. 重新加载扩展
3. 在 background console 中手动调用：
   ```javascript
   checkAndUpdateAppScript()
   ```

### 验证更新成功

1. 检查系统通知是否显示
2. 在定时消息管理页面检查版本信息
3. 调用 Web App 的 `getVersion` 端点验证：
   ```bash
   curl "YOUR_WEB_APP_URL?action=getVersion"
   ```

## 最佳实践

### 开发者

1. **谨慎修改 header 列名**
   - App Script 依赖列名进行动态映射
   - 修改列名可能导致功能异常

2. **测试后再发布**
   - 在测试环境验证新版本
   - 确保向下兼容

3. **记录更新日志**
   - 维护 CHANGELOG
   - 说明每个版本的变更

### 用户

1. **保持扩展更新**
   - 定期检查扩展更新
   - 自动更新机制依赖于扩展本身的更新

2. **关注更新通知**
   - 如果更新失败，会收到通知
   - 及时处理错误

3. **备份重要数据**
   - 定期导出 Google Sheet
   - 防止意外数据丢失

## 监控和日志

### Background Script 日志

```
🔄 检测到扩展更新，检查 App Script 是否需要更新...
✅ 找到 Scheduled Messages 配置，检查 App Script 版本...
🔄 发现新版本: 1.1.0，当前版本: 1.0.0
🚀 开始自动更新 App Script...
✅ App Script 已更新到版本 1.1.0
```

### UI 日志

```
发现新版本: 1.1.0
✅ 项目代码已更新
✅ 版本创建成功: 2
✅ 找到现有 deployment: AKfyc...
✅ 部署已更新（URL 保持不变）
✅ 配置已保存到 Chrome Storage
✅ 配置已同步到 Google Sheet
```

## 安全考虑

1. **权限控制**
   - 使用用户的 Google OAuth token
   - 仅更新用户自己创建的项目

2. **URL 不变**
   - 使用 update 而非 create
   - 防止链接失效

3. **数据完整性**
   - 更新前验证配置
   - 更新后同步配置

## 未来改进

1. **版本回滚**
   - 支持回退到之前的版本
   - 保留版本历史

2. **增量更新**
   - 仅更新变更的部分
   - 减少更新时间

3. **批量更新**
   - 支持管理员批量更新多个用户的 App Script
   - 企业级部署支持

4. **更新日志展示**
   - 在 UI 中显示版本更新日志
   - 让用户了解新功能

## 更新记录

- 2026-05-19：App Script 版本探测请求改为匿名 fetch，不携带 Chrome profile cookie，避免 Google 多账号登录态把公开 Web App 版本端点重定向到错误账号上下文后 404。
- 2026-05-19：App Script 版本更新到 2.8.4，Done 的未来单次消息会在轮询前自动恢复为 Active；部署后版本生效确认失败时，会尝试把 deployment 回退到升级前 versionNumber；管理页升级提示同步说明失败回退旧 deployment。
- 2026-05-18：App Script 版本更新到 2.8.2，cacheReleaseInfo dry-run 即使 POST JSON 格式错误也不会覆盖 Timeline Sync 最近同步诊断。
- 2026-05-17：版本探测返回非 JSON 时停止升级，避免把登录页、权限页或错误页误判成旧版脚本；升级 deployment 后增加 Web App 版本生效确认，未确认返回目标版本时不再同步最新配置，管理页同步展示确认步骤。
- 2026-05-15：模板版本增加 SemVer 合法性保护；deployment 预检失败时升级结果提供 Apps Script 项目入口，方便检查 Manage deployments。
- 2026-05-14：管理页升级提示补充升级前检查路径；检查失败或无法获取授权时清理旧的可升级状态，避免按过期检查结果继续操作。文档同步到模板版本 2.8.1。
- 2026-05-13：升级前正式 deployment 预检增加 Web App URL 匹配，避免多 deployment 项目中误更新其他 Web App；管理页提示同步说明 URL 匹配后才更新。App Script 版本更新到 2.7.9。
- 2026-05-11：App Script 可升级提示增加版本/额度/检查时间摘要；Project History 接近 200 上限时提前提供清理入口。文档同步到模板版本 2.7.8。
- 2026-05-11：App Script 版本更新到 2.7.8，未填写时间的 AsMe 消息不再进入执行器 8:00 兜底队列，保持 9:00 默认发送语义。
- 2026-05-10：App Script 版本更新到 2.7.7，任务成功收尾时会清空 `Next_Exec`，避免已完成消息继续显示旧的下次执行时间。
- 2026-05-10：App Script 版本更新到 2.7.6，空白 `AI_Endpoint` 不再把 JiraAutomation 规则误判为 AI 执行器消息。
- 2026-05-09：App Script 版本更新到 2.7.5，修复 Timeline 缓存保留完整项目对象时项目变量替换为空的问题。
- 2026-05-09：版本探测 URL 改为保留 Web App URL 既有查询参数后追加 `action=getVersion`，避免带参数的旧绑定检查失败。
- 2026-05-09：模板版本读取失败时停止检查/升级，不再缓存 `1.0.0` 兜底；管理页展示可重试的失败提示，避免静默误判为无需升级。
- 2026-05-08：App Script 版本更新到 2.7.4；升级前重新确认线上版本，已是最新时直接跳过脚本写入和版本创建；版本探测临时失败时停止升级，避免重复点击或网络/API 抖动消耗 Project History 版本额度。
- 2026-05-07：App Script 版本更新到 2.6.26，非法 Timeline Sync POST JSON 诊断会持久记录 Content-Type 和请求体大小，便于在扩展状态面板排查 Jira webhook 配置。
- 2026-05-06：升级前增加 Project History 版本额度预检；达到 200 个版本上限时，管理页直接引导用户打开清理页面，避免先覆盖脚本 HEAD 代码后才失败。
- 2026-05-06：App Script 版本更新到 2.6.23，Timeline Sync 会记录每个项目最近一次缓存写入尝试的安全诊断，便于 UI 展示失败原因。
- 2026-05-06：App Script 版本更新到 2.6.22，显式时间消息不再因分钟容差提前发送；非法 POST JSON 诊断同时保留 `.asJsonString` 修复指引和 releaseInfo 兜底格式。
- 2026-05-05：升级流程先预检正式 Web App deployment，再写入代码和创建版本，避免配置异常时消耗 Apps Script 版本额度。
- 2026-05-05：App Script 版本更新到 2.6.20，修复 Jira 执行回写在 Sheet 排序或插行后可能误信旧 rowIndex 的问题。
- 2026-05-04：补强 App Script 版本比较，支持 `v` 前缀、预发布版本和 build metadata；管理页增加可升级提示条，升级确认中展示当前/最新版本。

---

**相关文件：**
- `src/scheduled-messages/app-script-template.gs` - App Script 模板
- `src/scheduled-messages/AppScriptUpdater.ts` - 更新器实现
- `src/scheduled-messages/SheetInitializer.ts` - 初始化时保存 deploymentId
- `src/background.ts` - 自动检测逻辑
- `src/scheduled-messages/ScheduledMessagesManager.tsx` - UI 更新按钮

**参考文档：**
- [Google Apps Script API - deployments.update](https://developers.google.com/apps-script/api/reference/rest/v1/projects.deployments/update)
- [Google Apps Script Versions](https://developers.google.com/apps-script/guides/versions)
- [Google Apps Script API - versions.list](https://developers.google.com/apps-script/api/reference/rest/v1/projects.versions/list)
- [Semantic Versioning 2.0.0](https://semver.org/)
