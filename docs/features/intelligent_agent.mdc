---
description: 
globs: 
alwaysApply: false
---
# 智能Agent系统设计

*最后更新: 2024-04-17*

## 功能概述

智能Agent是一个通用的信息分析框架，能够分析各种类型的输入（如聊天消息、项目数据、会议记录等），并通过思考-行动循环结合多种工具生成结构化的分析结果。系统设计强调灵活性、可扩展性和类型安全，使其能够适应不同领域的信息分析需求。

## 功能说明

智能Agent系统是一个灵活的信息分析框架，具有以下核心能力：

1. **通用信息分析**
   - 分析各种类型的输入数据（消息、项目、会议等）
   - 根据分析类型生成结构化、类型安全的结果
   - 支持不同分析深度（快速、标准、深入）

2. **多工具协同处理**
   - 动态决定工具调用顺序和使用策略
   - 整合多个工具的执行结果形成综合分析
   - 支持扩展新的工具而无需修改核心逻辑

3. **可配置的分析行为**
   - 通过配置控制分析深度、工具偏好
   - 支持自定义提示模板
   - 提供上下文注入能力，影响分析行为

4. **强类型分析结果**
   - 不同分析类型返回特定结构的结果对象
   - 减少通用字段容器（如enrichedData）的使用
   - 确保领域特定信息直接映射到结构化字段

## 系统架构

```
智能Agent系统
├── 核心组件
│   ├── IntelligentAgent类 - 主要智能体
│   ├── 工具注册表 - 可用工具的注册中心
│   └── 处理流程管理器 - 处理思考-行动循环
│
├── 接口定义
│   ├── 基础分析结果 (BaseAnalysisResult) - 所有分析结果的基类
│   ├── 类型特定结果 (MessageAnalysisResult, ProjectAnalysisResult等)
│   ├── 分析配置 (AnalysisConfig) - 控制分析行为 
│   └── 分析上下文 (AnalysisContext) - 提供环境信息
│
├── 工具集
│   ├── 实体提取 - 提取消息中的人物、项目等实体
│   ├── 历史消息搜索 - 查询相关历史消息
│   ├── 消息存储 - 将消息存储到向量数据库
│   ├── 消息通知 - 通过bot发送通知
│   ├── JIRA查询 - 获取JIRA任务信息
│   ├── 组织架构查询 - 获取人员的组织关系
│   ├── 发布任务查询 - 查询本月发布任务
│   └── Sprint数据查询 - 查询Sprint进度和bug数据
│
└── 可视化组件
    ├── AgentVisualizer - 显示详细的思考过程
    ├── AgentFlowVisualizer - 流程图可视化
    └── AgentResultSummary - 处理结果摘要
```

## 设计目标与原则

1. **领域特定处理与通用逻辑分离**：核心思考-行动循环保持通用，领域特定逻辑通过特定的提示构建和结果构建实现。

2. **类型安全**：使用类型化接口定义不同分析结果，避免通用容器（如enrichedData）存储特定领域的结果。

3. **可配置与可扩展**：系统设计支持轻松添加新的分析类型和结果格式，同时保持核心逻辑的一致性。

4. **渐进式迁移**：设计支持与现有系统的兼容性，允许渐进式迁移。

## 核心接口设计

### 基础分析结果接口

```typescript
/**
 * 基础分析结果接口 - 所有分析结果的共同基础
 */
interface BaseAnalysisResult {
  confidence: number;          // 分析可信度
  summary: string;             // 分析总结
  thoughtProcess?: ThoughtStep[]; // 思考过程
  metaData: {                  // 元数据
    llmCallCount: number;      // LLM调用次数
    llmCallTokens: number;     // Token使用量
    usedTools: string[];       // 使用的工具
    timestamp: number;         // 分析时间戳
  };
}
```

### 消息分析结果接口

```typescript
/**
 * 消息分析结果接口 - 专门用于聊天消息
 */
interface MessageAnalysisResult extends BaseAnalysisResult {
  type: 'message';              // 结果类型标识符
  isImportant: boolean;         // 消息是否重要
  shouldStore: boolean;         // 是否应存储
  shouldNotify: boolean;        // 是否应通知
  notificationPriority?: 'high' | 'medium' | 'low'; // 通知优先级
  reasonsToStore?: string[];    // 存储理由
  replyAdvice?: string;         // 回复建议
  messageContext?: {            // 消息上下文
    messageIndex?: number;
    groupIndex?: number;
    groupId?: string;
    groupName?: string;
  };
  entities?: {                   // 提取的实体
    people: any[];
    topics: any[];
    projects: any[];
    // ...其他实体类型
  };
}
```

### 项目分析结果接口

```typescript
/**
 * 项目分析结果接口 - 专门用于项目分析
 */
interface ProjectAnalysisResult extends BaseAnalysisResult {
  type: 'project';              // 结果类型标识符
  projectId: string;            // 项目ID
  projectName: string;          // 项目名称
  riskLevel: 'normal' | 'attention' | 'risk'; // 风险等级
  suggestions: {                // 更新建议
    status?: {
      current: string;
      suggested: string;
      reason: string;
    };
    owner?: {
      current: string;
      suggested: string;
      reason: string;
    };
    track?: {
      current?: string;
      suggested: string;
      reason: string;
    };
    comments?: {
      current?: string;
      suggested: string;
      reason: string;
    };
  };
  sourceInfo?: {                // 信息来源
    jiraStatus?: string;
    jiraComments?: string[];
    chatHistory?: any[];
  };
  nextActions?: string[];       // 建议的下一步行动
}
```

### 分析配置接口

```typescript
/**
 * 分析配置接口 - 控制分析行为
 */
interface AnalysisConfig {
  type: 'message' | 'project' | 'meeting' | string; // 分析类型
  maxActions?: number;          // 最大行动次数
  preferredTools?: string[];    // 优先使用的工具
  customPrompts?: {             // 自定义提示
    analysis?: string;          // 分析提示
    thinking?: string;          // 思考提示
  };
  analysisDepth?: 'quick' | 'normal' | 'deep'; // 分析深度
}
```

### 分析上下文接口

```typescript
/**
 * 分析上下文接口 - 提供分析环境信息
 */
interface AnalysisContext {
  currentUser?: string;         // 当前用户
  timestamp?: number;           // 分析时间
  groupInfo?: {                 // 群组信息
    id?: string;
    name?: string;
  };
  relatedItems?: any[];         // 相关项目
  concernedRules?: string[];    // 关注规则
  // 其他可能的上下文信息...
}
```

## 信息处理流程

### 总体分析流程

```
┌──────────────┐     ┌────────────────┐    ┌────────────────┐
│    输入      │     │  分析配置      │    │  分析上下文    │
│  input: any  │     │AnalysisConfig  │    │AnalysisContext │
└──────┬───────┘     └───────┬────────┘    └───────┬────────┘
       │                     │                     │
       └─────────────────────┼─────────────────────┘
                             │
                 ┌───────────▼───────────┐
                 │  IntelligentAgent    │
                 │     analyze()        │
                 └───────────┬───────────┘
                             │
                 ┌───────────▼───────────┐
                 │    根据config.type    │
                 │    选择分析方法      │
                 └───────────┬───────────┘
                             │
           ┌─────────────────┼─────────────────┐
           │                 │                 │
┌──────────▼─────────┐┌──────▼─────────┐┌──────▼──────────┐
│  analyzeMessage   ││ analyzeProject  ││  analyzeMeeting  │
└──────────┬─────────┘└──────┬─────────┘└──────┬──────────┘
           │                 │                 │
           └─────────────────┼─────────────────┘
                             │
                 ┌───────────▼───────────┐
                 │   normalizeInput()    │
                 │ 标准化输入数据格式    │
                 └───────────┬───────────┘
                             │
                 ┌───────────▼───────────┐
                 │   initialAnalysis()   │
                 │ 首次LLM分析，提取关键 │
                 │ 信息、实体、意图等    │
                 └───────────┬───────────┘
                             │
                      ┌──────▼──────┐
                      │   进入循环   │
                      └──────┬──────┘
                             │
                 ┌───────────▼───────────┐
                 │       think()         │
                 │ 思考下一步行动，选择  │
                 │ 工具或决定结束       │
                 └───────────┬───────────┘
                             │
                 ┌───────────▼───────────┐
                 │  nextAction是什么?    │
                 └───────────┬───────────┘
                             │
               ┌─────────────┴─────────────┐
               │                           │
    ┌──────────▼────────────┐   ┌──────────▼────────────┐
    │      use_tool         │   │       finish          │
    └──────────┬────────────┘   └──────────┬────────────┘
               │                            │
    ┌──────────▼────────────┐               │
    │    executeTools()     │               │
    │  执行选定工具获取     │               │
    │  额外信息            │               │
    └──────────┬────────────┘               │
               │                            │
               └────────────────────────────┘
                             │
                             │
                 ┌───────────▼───────────┐
                 │  根据config.type     │
                 │  构建特定类型结果    │
                 └───────────┬───────────┘
                             │
           ┌─────────────────┼─────────────────┐
           │                 │                 │
┌──────────▼─────────┐┌──────▼─────────┐┌──────▼──────────┐
│ buildMessageResult ││buildProjectResult││buildMeetingResult│
└──────────┬─────────┘└──────┬─────────┘└──────┬──────────┘
           │                 │                 │
           └─────────────────┼─────────────────┘
                             │
                 ┌───────────▼───────────┐
                 │    返回分析结果       │
                 │    AnalysisResult     │
                 └─────────────────────────┘
```

### 思考-行动循环

智能Agent系统基于"思考-行动"循环，而不是预定义的线性流程。每次循环包括：

1. **思考阶段**：Agent评估当前状态，思考下一步行动
2. **决策阶段**：决定是执行某个工具还是结束处理
3. **执行阶段**：如果选择执行工具，则调用相应工具并收集结果
4. **更新阶段**：更新内存状态，记录执行结果

整个过程由LLM驱动，每次决策都通过提示工程引导LLM做出合理的判断。循环继续直到Agent决定已收集足够信息做出最终分析，或者达到配置的最大行动次数。

### LLM提示构建流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                      LLM提示构建流程                                │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 1. 根据分析类型选择基础模板                                         │
│    - 消息分析基础模板                                              │
│    - 项目分析基础模板                                              │
│    - 会议分析基础模板                                              │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 2. 注入分析配置参数                                                 │
│    - 分析深度 (analysisDepth)                                      │
│    - 工具偏好 (preferredTools)                                     │
│    - 自定义提示 (customPrompts)                                    │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 3. 注入上下文信息                                                   │
│    - 当前用户 (currentUser)                                         │
│    - 关注规则 (concernedRules)                                      │
│    - 群组/项目信息 (groupInfo)                                     │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 4. 添加可用工具描述                                                 │
│    - 工具ID、名称和描述                                            │
│    - 参数定义和示例                                                │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 5. 添加输入数据                                                    │
│    - 标准化后的输入内容                                            │
│    - 思考循环中：添加历史工具执行结果                               │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 6. 添加期望输出格式定义                                             │
│    - JSON结构描述                                                  │
│    - 字段说明和约束                                                │
└─────────────────────────────────────────────────────────────────────┘
```

## 工具调用策略

工具调用不是预先定义的固定顺序，而是基于分析内容动态决定的。例如：

- 如果分析项目，Agent可能会优先选择查询JIRA
- 如果分析涉及多个人物的消息，Agent可能会选择查询组织架构
- 如果分析提到截止日期，Agent可能会查询发布计划

工具调用策略受到以下因素的影响：

1. **分析类型(AnalysisConfig.type)**：不同类型的分析会倾向于使用不同的工具集合
2. **优先工具(AnalysisConfig.preferredTools)**：允许在配置中指定偏好的工具
3. **分析深度(AnalysisConfig.analysisDepth)**：深度分析可能会调用更多的工具获取全面信息
4. **LLM决策**：基于对输入内容的理解，LLM会动态决定需要哪些工具

这种灵活性使Agent能够根据具体场景"思考"出最合适的工具调用路径，而不是按照固定流程执行。

## 使用示例

### 消息分析示例

```typescript
// 分析聊天消息
const result = await intelligentAgent.analyze(
  messageContent,
  {
    type: 'message',
    analysisDepth: 'normal',
    preferredTools: ['historySearch', 'entityExtractor']
  },
  {
    currentUser: 'user123',
    concernedRules: ['项目延期风险', '紧急会议通知']
  }
);

// 使用类型化结果
if (result.type === 'message') {
  const msgResult = result as MessageAnalysisResult;
  
  if (msgResult.shouldStore) {
    // 存储消息
  }
  
  if (msgResult.shouldNotify) {
    // 发送通知
  }
}
```

### 项目分析示例

```typescript
// 分析Google Slides中的项目
const results = await Promise.all(
  projectsData.map(project => 
    intelligentAgent.analyze(
      project,
      {
        type: 'project',
        analysisDepth: 'deep',
        preferredTools: ['jiraQuery', 'historySearch']
      },
      {
        currentUser: 'current-user',
        timestamp: Date.now()
      }
    )
  )
);

// 处理分析结果
const updateSuggestions = results
  .filter(result => result.type === 'project')
  .map(result => result as ProjectAnalysisResult)
  .filter(result => 
    result.suggestions.status || 
    result.suggestions.owner || 
    result.suggestions.comments
  )
  .map(result => ({
    projectId: result.projectId,
    projectName: result.projectName,
    suggestedStatus: result.suggestions.status?.suggested,
    // ... 其他字段
  }));
```

## 兼容性与迁移策略

为确保与现有系统的兼容性，智能Agent系统设计了以下迁移策略：

1. **保留旧接口**：保留但标记为弃用`MessageProcessResult`和相关处理方法。

2. **兼容层**：实现从新接口到旧接口的转换函数，使现有代码能够继续工作。

3. **渐进式迁移路径**：
   - 第一阶段：实现新API和兼容层
   - 第二阶段：更新特定领域分析代码使用新API
   - 第三阶段：逐步更新所有调用代码
   - 第四阶段：标记旧API为deprecated
   - 第五阶段：在主要版本更新时移除旧API

### 兼容层示例

```typescript
// 兼容层转换函数
private convertToOldFormat(result: AnalysisResult): MessageProcessResult {
  if (result.type !== 'message') {
    throw new Error('Cannot convert non-message result');
  }
  
  const msgResult = result as MessageAnalysisResult;
  return {
    isImportant: msgResult.isImportant,
    shouldStore: msgResult.shouldStore,
    shouldNotify: msgResult.shouldNotify,
    confidence: msgResult.confidence,
    summary: msgResult.summary,
    reasonsToStore: msgResult.reasonsToStore,
    // ... 其他字段映射
    
    // 旧版enrichedData字段处理
    enrichedData: {
      entities: msgResult.entities,
      ...msgResult.metaData,
      // 其他需要保留的字段
    }
  };
}
```

## 关键优势

1. **强类型支持**：每种分析类型都有明确的接口定义，提高代码安全性。

2. **领域特定结构**：不同类型的分析结果有各自的数据结构，避免通用字段混用。

3. **可配置性**：通过配置控制分析行为和工具偏好，无需修改核心代码。

4. **可扩展性**：易于添加新的分析类型和结果格式，支持更多应用场景。

5. **清晰的API**：提供简单明了的公共API，隐藏内部复杂性。

## 未来计划

1. **更多分析类型支持**：添加更多预定义的分析类型，如代码审查、数据分析等。

2. **结果后处理扩展**：提供结果后处理钩子，支持用户自定义处理逻辑。

3. **批量分析优化**：提高批量分析的性能和资源利用率。

4. **工具调用策略增强**：实现更智能的工具调用决策策略，减少不必要的工具调用。

5. **分析质量评估**：添加分析结果质量评估机制，持续改进分析性能。 