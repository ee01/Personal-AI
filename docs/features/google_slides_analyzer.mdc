---
description: Google Slides 项目分析器
globs:
alwaysApply: false
---

# Google Slides 项目分析器

当前功能说明以 `docs/features/google_slides_analyzer.md` 为准。

同步要点：

- 从 Google Slides 页面或 popup 触发分析当前 slide。
- 结合表格/文本/LLM 分析、Jira 查询和项目上下文生成更新建议；Jira key 支持带数字项目前缀，备注列支持 Comments、Highlights、Actions、Next steps 等常见命名。
- 结果页展示建议、置信复核状态和已选字段数量，不保存 OAuth token。
- 结果页可按全部、已选、需复核、无法写回筛选建议，并支持恢复“高可信且有来源证据”的默认选择或清空选择。
- 写回前必须由用户勾选字段并确认；高可信且有可见来源的字段可默认选中，低可信或缺少来源的字段需人工复核。
- 每条建议展示来源证据摘要；缺少可见来源时不会默认勾选。
- 状态、负责人和赛道在结果页展示与写回前都会做大小写与空白归一化比较，避免仅格式差异触发更新或默认勾选。
- 表格单元格写回使用明确删除范围和插入位置；备注建议按行剔除已存在内容，避免重复追加。
- 缺失可写表格列的建议不会进入可勾选写回，并会在结果页提示原因。
- 写回完成后展示实际写入字段数并清空选择；初始数据加载、无效请求或父窗口无响应都会给出错误/超时提示，不让结果页卡住。
