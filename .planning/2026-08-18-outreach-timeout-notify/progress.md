# Progress

- Decision Center path confirmed; answering outreach_followup does not resume the session.
- Timeout/escalated now share the Bot receipt path; body includes follow-up status, Glip jump link, and continue marker.
- Continue-followup API keeps the original thread and wait window.
- Glip Bot receipt shows a continue bar; session detail/list open the interval+count form.
- Validation: outreachResultNotice + outreachEngine tests, message-reaction tests, webpack compile (pre-existing unused `probe` warning), `npm run deploy:memory` succeeded.
