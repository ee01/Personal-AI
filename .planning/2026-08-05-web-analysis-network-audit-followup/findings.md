# Findings: Web Analysis And Background Network Audit Follow-up

## Requirements

- Compare current `agentThinking.ts` webpage analysis against a focused single `llm.ts` request.
- Explain whether Agent Thinking performs tool calls and whether that is useful or risky.
- Diagnose same-snapshot reruns without active user interaction and assess debounce/hash/single-flight fixes.
- Run controlled real model examples and judge expected product value and prompt quality.
- Classify every other captured request by LLM/token consumption, duplication cause, and likely trigger.
- Explain notification batching/mode, background cache/single-flight, recent 500 Logs, and incremental Sheet/Glip impact.
- Implement the approved high-value changes while avoiding low-benefit non-LLM churn with material side-effect risk.

## Evidence Ledger

- Previous capture audit: 336 direct OpenAI calls; 335 were webpage-analysis templates, 89 initial runs, 246 planning rounds, 73 repeated same normalized 2,000-character snapshots.
- Current source has a 5-second analysis interval check before `setTimeout`, but no analysis timer handle and no callback-time recheck.
- Content script sends `WEB_INTELLIGENCE_ANALYSIS` fire-and-forget; background starts a fresh `IntelligentAgent` with `analysisDepth: normal` and no cross-run content hash/in-flight key.
- Agent webpage analysis always makes one initial LLM call, then can make up to 3 decision calls; decision output may request tools.
- The thinking loop exposes the entire registered tool catalog to the model. It validates each proposed call, blocks unknown/missing-parameter calls, deduplicates identical calls within one agent run, and requires approval for tools marked write/notify/delete. Read-only registered tools may execute automatically.
- Therefore Agent Thinking is not merely deeper text analysis: it adds an action-selection loop and can execute approved/read-only tools. The capture's 246 thinking calls for 89 starts show the loop was commonly paying for about 2.76 extra LLM decisions per page start.
- `getRecentPushLogs(500)` reads the whole `Logs` worksheet, then slices rows 1..500 locally. The number means “take at most the first 500 data rows returned,” not a server-side recent/changed-only query; correctness also depends on the sheet's row ordering.
- The marker refresh also calls `getAllMessages()`. That method reads the whole `Messages` worksheet and may write back missing IDs or normalized `Push_Method` values during what appears to callers as a read path.
- Passive context recall is proxied by background to memory-service; the extension call path itself does not call `llm.ts`. It is a retrieval request, though backend internals should be audited separately before claiming zero server-side inference.
- Captured direct OpenAI bodies used `gpt-5-nano` with no explicit output-token cap.
- The live comparison used the captured `gpt-5-nano` route with three synthetic fixtures and did not execute tools. The repo `.env` OneAPI credential was attempted first but returned 401, so it produced no model result.
- Jira fixture: current initial call used 2,194 total tokens and the first thinking call used 1,522; the proposed single call used 3,083. Current output contaminated extracted entities with every project/person from user context. The thinking call proposed `jiraQuery` with an unsupported `jql` parameter.
- Chat fixture: current initial + first thinking used 2,615 + 2,038 total tokens; proposed single call used 4,345. Current output again copied unrelated projects/people from user context. Thinking proposed `jiraQuery.params.jql` and `historySearch.params.query`; the former is silently ignored and falls back to a broad recent-issues search, while the latter is blocked because required `content` is missing.
- Static-noise fixture: current initial + first thinking used 3,137 + 2,341 tokens and incorrectly returned `shouldStore=true`, high priority, and a fabricated cross-project retention reason; proposed single call used 1,707 and correctly returned `skip` with no durable facts.
- Across the three fixtures, current initial + just one thinking step consumed 13,847 total tokens versus 9,135 for the proposed single call (34.0% lower). Production current flow can make up to two additional thinking calls, so this understates the multi-step cost gap.
- The proposed single-call prompt was directionally more precise, but still too verbose on valuable pages (especially the chat fixture's 3,520 reasoning tokens) and added an unnecessary enrichment hint on the static page. The production prompt should cap facts/actions, require empty enrichment hints on `skip`, and set an explicit output/reasoning budget supported by the chosen model.
- `jiraQuery` accepts no required parameter and ignores unknown parameters; a model-proposed `jql` therefore passes validation and executes the default `updated >= -30d` search. Tool parameter schemas need `additionalProperties: false` behavior plus one-of validation (`issueId|issueIds|keywords`).
- `historySearch` requires `content`; the model naturally proposed `query`, so that call is blocked. Prompt-visible schema and runtime validation are misaligned with natural model output.
- Despite background/content-script logs saying the page was “processed and stored,” the webpage path only returns the `WebpageAnalysisResult`; the content script only logs it. The only currently registered tools are read-only `historySearch` and external-read `jiraQuery`; no storage tool is registered. The captured webpage LLM spend therefore did not itself create a durable, searchable memory.
- Capture quality anomaly: 66 notification feed calls with requested limit 20 can yield at most 1,320 poll-loop items, but the capture contains 2,537 one-event `delivered` POSTs. Current client has no HTTP retry. The excess cannot be attributed to the visible poll loop alone; likely candidates include another/older caller, multiple extension instances/workers, or capture composition. Add caller version, extension instance, worker run, poll run, and batch identifiers before claiming one root cause.
- The attachment has 5,060 curl starts, of which 5,035 contain a reconstructable URL; 25 malformed/truncated starts cannot be safely classified. Endpoint totals should use 5,035 as the auditable denominator and disclose the 25-record gap.
- Source-memory candidate scoring is deterministic server code, not LLM. The page capture evaluator intentionally re-scores up to every 30 seconds while dwell time approaches 90s/240s/480s thresholds, so most of its 208 requests are changing interaction snapshots rather than identical duplicates. It should score only on threshold crossings or material interaction changes.
- Composer Assist has timer replacement, request signatures, stale-snapshot checks, and per-session in-flight gating. Its low exact duplicate rate is consistent with draft/focus revisions, not the webpage-analysis timer race. Backend LLM use is conditional: web-agent prompt compile uses LLM; ordinary RingCentral compose can remain retrieval/rule-only under production defaults.
- The 84 captured `/recall` requests omitted `blockTypes`, so `ActiveRecallService` takes its evidence-only no-LLM branch. Passive `/context-recall` is explicitly designed with no LLM in the path.
- Notification delivery is a deterministic database receipt write. It does not need an LLM. Poll currently omits `deliveryMode`, so the server defaults to `retry_after_cooldown`; for a Chrome incremental poller, explicitly selecting `incremental` is the safer semantic unless repeated reminders are an intentional product behavior.
- `getRecentPushLogs(500)` is misnamed/ambiguous: it downloads the entire Logs sheet and keeps the first 500 data rows. Unless the sheet is maintained newest-first, this is not “recent.”
- Confirmed the missing ordering contract in the current Apps Script: `appendLog()` inserts every new record after the header and writes it to row 2. `Logs` is therefore intentionally newest-first. The current first-500 selection is logically correct; only the full-tab transport is wasteful. The low-risk redesign is a bounded header-plus-first-`limit` A1 range read, not a tail cursor or client-side sort.
- The worktree contains substantial user changes, including overlapping edits in `src/background.ts`, `src/contentScriptWebIntelligence.ts`, `src/scheduled-messages/ScheduledMessageService.ts`, and `src/sheet.ts`. Implementation patches must stay narrow and preserve those changes.
- Scope rule for the implementation pass: prioritize LLM/token reduction and clearly redundant network traffic. Do not introduce durable queues, new persistence semantics, or broad incremental-sync machinery unless the current contract makes the improvement low-risk and directly testable.
- The safest way to merge focused LLM analysis into Memory Capture is after `MEMORY_CAPTURE_SCORE_PAGE` reports an eligible ordinary webpage. Visual-memory candidates retain their existing deterministic/manual flow, and LLM failure falls back to the existing candidate result instead of blocking capture.
- The existing `pageCaptureTimer` already provides timer replacement/debounce. Reusing it and removing the separate automatic analysis timer avoids two competing page-analysis schedulers; significant DOM changes schedule a candidate reevaluation only after an intent threshold is present.
- Notification delivery batching alone has a repeat-on-failure hole under incremental feed semantics. A bounded local outbox can close it without changing server persistence: flush receipts before polling; if the flush still fails, skip that poll so the same items are not displayed again while their delivery receipts are pending.
- Broad Messages/Glip incremental caching is intentionally deferred in this pass. Unlike newest-first Logs, editable/reorderable Messages and remote marker snapshots lack a safe local change cursor; changing them now would add stale-marker behavior and migration-write coupling for a non-LLM optimization.
- Implementation now reads only `Logs!1:{limit+1}`. Because the writer inserts each new log at row 2, `getRecentPushLogs(500)` is both logically newest-first and transport-bounded; no cursor, client sort, or full reconciliation change is needed for this path.
- The configured HTTPS memory-service hostname currently fails during TLS/SNI negotiation while the HTTP health endpoint succeeds. Migrating the extension default to HTTPS before fixing the gateway certificate would be a breaking change, so it remains an operational follow-up.
- Focused extension E2E passes for webpage-analysis reuse and context-recall cache behavior. The combined legacy E2E still fails at a Jira/Compose Assist context-bubble assertion, which is kept as a separate overlapping regression rather than attributed to the new cache path.
- On 2026-08-12 the complete E2E reproduced the same boundary with stronger evidence: the Jira fixture emitted exactly one context-recall request, but `.pai-context-bubble` never became visible within 12 seconds. Request dedupe is therefore working; the remaining defect is downstream response/display suppression or rendering.
- The current worktree contains extensive newer work. The only task-adjacent dirty runtime file is `src/background.ts`, and its visible diff is newer Roadmap/Agent Task work, so any repair must avoid reverting or rewriting that overlap.
- Jira root cause: the read-state exception in `shouldSuppressContextBubbleForComposerAssist(payload)` was correct, but the Compose Assist DOM-mutation and visibility-event callers omitted `payload`. They therefore treated a pre-rendered Jira icon as an active composer, invalidated the pending request id, and discarded the otherwise valid recall response. Passing the current Jira context through both callers restores the intended contract.
- After fixing Jira, the full E2E exposed that the new background/session context cache was also caching `selected_text`. This conflicts with the active-selection contract: reselecting the same text is an explicit user action and should re-read current memory state. Cache reuse must remain limited to passive scene projection; `selected_text` bypasses completed-result cache while retaining ordinary request handling.
- Provider discovery on 2026-08-12: `.env.development` selects `dify` and has no direct OpenAI key; `.env` selects the internal OneAPI model `ringcentral-deepseek-v3.2:8b`, but a credential-safe `/models` probe returns HTTP 401. Local Ollama is not running. The stored local provider credential is therefore not a valid basis for a new live prompt claim.
- `src/llm.ts` still constructs browser-capable OpenAI clients with `dangerouslyAllowBrowser: true`. Even though the passive page path now makes one call, any long-lived provider key bundled into the extension remains a security boundary that should be replaced by a trusted service route or a short-lived/restricted token.

## 2026-08-12 Live Provider And Transport Closure

- The deployed memory-service has a configured Anthropic credential and `claude-sonnet-4-6`. A credential-safe `/ask` probe returned HTTP 200, confirming the provider can complete a real request.
- Four synthetic webpage fixtures were sent through the exact production `buildPassiveWebpageAnalysisPrompt` and then through `normalizePassiveWebpageAnalysisResult`. All four passed: Jira blocker and decision chat were retained with page-grounded facts, while the static documentation shell and login page were skipped.
- Live usage was 760-788 input tokens and 99-518 output tokens per page; latency was 2.3-7.4 seconds. This supports the focused one-call design and did not reveal a reproducible reason to expand the prompt or restore Agent Thinking.
- `memory.xmnup.com` resolves to private `10.32.56.212` even through public resolvers. Nginx Proxy Manager proxy host 4 listens only on HTTP and has no certificate. Its default TLS server rejects unknown SNI, which explains the current `unrecognized name` failure.
- Nginx Proxy Manager contains only one usable certificate: a DigiCert certificate whose SAN covers `milo.xmnup.com` only. It cannot be safely reused for `memory.xmnup.com`; issuing/importing a new certificate requires control of the external certificate or DNS-validation workflow.
- The supplied network capture contains complete bearer credentials: an OpenAI key used against `api.openai.com`, Google OAuth access tokens, and a Jira bearer token. They were classified without printing or replaying them. The old Google access tokens are naturally short-lived; the OpenAI and Jira credentials must be revoked/rotated in their provider consoles, then the replacement values must be installed outside the capture/repository.
- `webpack.prod.cjs` currently serializes the full parsed `.env` into the extension bundle, and the shared `llm.ts` path uses direct OpenAI/Groq/Dify credentials for more than webpage analysis. Removing only the passive webpage key use would not close the general client-secret boundary. Routing page analysis over the current HTTP memory-service endpoint would also expose page content in transit, so a safe migration order is: trusted HTTPS gateway, server-side focused analysis endpoint, short-lived/device authorization, then removal of provider secrets from extension builds.
- The dirty worktree contained an untracked `.env.bak`. Its contents were not read or deleted; `.gitignore` now excludes `.env.bak` and `.env.*.bak` so local credential backups cannot be accidentally staged.

## Controlled Test Safety

- Do not reuse captured Jira/chat bodies in live model tests.
- Do not instantiate a path that may execute Agent Thinking tools.
- Use synthetic fixtures and direct prompt calls only; record response usage and latency without printing credentials.

## Issues Encountered

| Issue | Resolution |
|---|---|
| Existing root planning files are unrelated and dirty | Use isolated plan directory and preserve existing files |

## 2026-08-12 Current Frequency Recheck Scope

- Real Nginx/memory-service logs can prove current requests reaching `memory.xmnup.com`, but cannot observe direct browser-to-OpenAI/Dify traffic.
- The current-build simulation must use synthetic pages and mock endpoints so it exercises real extension scheduling/dedupe code without sending private page contents or incurring LLM cost.
- Any final estimate must label three distinct sources: historical captured traffic, current real gateway logs, and controlled current-build simulation/source cadence.

## 2026-08-12 Real Gateway Traffic

- Current Nginx Proxy Manager host-4 access log contains 15,964 successful/attempted memory-service requests across 50.1 hours (318.7/hour). The latest 24-hour slice contains 6,896 requests (287.5/hour), and the latest six-hour slice contains 1,998 (334.4/hour).
- The latest 24 hours are dominated by deterministic/non-LLM paths: source-memory candidate scoring 1,174; Glip marker, concerned-item, and follow-thread reads about 994-999 each; context recall 869; profile items 713; keystone event receipts 467 across visible brief IDs; Composer Assist 299; notification feed 99. Notification delivery is now only 2 batched POSTs.
- The server log proves that total memory-service traffic is still high, even though notification delivery fan-out collapsed. It does not prove direct LLM traffic because OpenAI/Dify requests bypass this gateway.
- A persistent overnight floor near 154-178 requests/hour indicates one or more background/tab-driven loops remain active without foreground user interaction. Daytime peaks reached 679-681 requests/hour.
- The 24-hour interval distribution points to repeated MV3 background startup work, not just nominal alarms: profile/items ran 713 times with a 74-second median gap; glip markers, concerned-items, and follow-thread-hits each ran about 994-999 times with roughly 60-64-second median gaps. Their declared alarm cadences are only five minutes.
- Background module top-level code runs `syncStoredUserIdentityToMemory()`, schedules an immediate Glip marker refresh, and starts `TaskScheduler`; each fresh MV3 service-worker lifetime resets in-memory `isInitialized` state, and TaskScheduler startup calls concerned-item snapshot/hit synchronization again. This is the strongest source-backed explanation for the overnight request floor.
- Notification feed is healthy and deterministic: 99 requests/24h with a 900-second p50/p90 gap, exactly matching the 15-minute alarm. Notification delivery is now two batched receipts rather than one POST per event.
- Frontend LLM calls are independently observable through `UsageTracker`: each `handleLLMRequest` records capability/feature/model/tokens and the background batches those events to `/usage/telemetry`. Querying the deployed analytics store can therefore fill the direct-LLM gap left by Nginx access logs, subject to the tracker being present in the installed extension version.

## 2026-08-12 Real LLM Usage And Current Browser State

- The deployed analytics store's latest 24-hour slice contains 452 frontend LLM attempts: 292 `passive_webpage_memory_analysis` attempts on `deepseek-ai/deepseek-r1` (all `network` errors, zero reported tokens), 8 legacy `webpage_initial_analysis` calls, 24 legacy `webpage_thinking` calls, and 128 `message_analysis` calls. The three successful legacy/message groups reported 653,958 tokens in total. Backend rows were excluded from this frontend total because several were generated by the audit probes themselves.
- The legacy webpage pair still has the old 1+3 shape: 8 initial analyses and 24 thinking calls. The current passive path does not call Agent Thinking, so these rows indicate stale/legacy runtime overlap during the measured day rather than the new source path itself.
- Chrome 151 is currently running the unpacked Personal AI extension from this checkout's `dist` directory. The same unpacked extension path is also registered in Chrome Canary profiles, so multiple browser/profile runtimes are possible, although almost all gateway requests in the measured window used the Chrome 151 user agent.
- Seven ordinary HTTP(S) tabs were open in the connected Chrome profile at inspection time; this is not a many-dozens-of-tabs explanation for the request rate. The active RingCentral tab visibly contained one Context Bubble and one page-memory capture chip.
- The active RingCentral tab emitted a fresh `单次网页分析不可用 ... Failed to fetch` warning at 2026-08-12 12:19 local time from `contentScriptWebIntelligence.js`. This proves the current one-call passive-analysis path is live and currently failing at the provider fetch boundary. Repeated entries returned by the browser log adapter at the same millisecond were not counted as separate provider calls because that adapter can duplicate console records.
- The current `dist/contentScriptWebIntelligence.js` and `dist/background.js` were rebuilt at 11:41 and 11:49 local time. Chrome's extension page was last foregrounded at 09:47, so file timestamps alone cannot prove every already-open tab was reinjected after the build; current-tab console evidence is therefore stronger than build-path inspection for the active RingCentral tab.

## 2026-08-12 Current-Build Simulation And Cadence Model

- Focused mock E2E `PERSONAL_AI_E2E_FOCUS=page-analysis npm run verify:webpage-memory-detection:e2e` passed. Its assertions prove that one semantic page snapshot uses one focused LLM call across repeated candidate evaluation, site-control churn, and a second tab with the same snapshot.
- Focused mock E2E `PERSONAL_AI_E2E_FOCUS=context-cache npm run verify:webpage-memory-detection:e2e` passed. It proves an exact context reload reuses the background/session cache, while a different URL creates one new recall request.
- Success behavior is bounded as designed: a successful non-skip analysis is cached for 45 minutes, a successful skip for 20 minutes, and concurrent identical requests join one in-flight promise.
- Failure behavior is not bounded the same way: provider failures are neither negatively cached nor backed off. On a stable eligible page, the source candidate is rescored at about 45 seconds and then up to every 30 seconds until the eight-minute long-read threshold, for roughly 16 deterministic score POSTs. If each score remains eligible and the provider remains unavailable, the same stable semantic snapshot can attempt the LLM again each time. Later significant DOM changes can schedule further attempts even after the long-read timer stops.
- The 1,174 `/source-memory/candidates/score` calls in 24 hours are deterministic server-side scoring, not LLM calls. Their 48.9/hour observed rate is consistent with the current repeated long-read recheck design across a small number of active pages.
- With one long-lived MV3 worker and no active pages, the declared nominal memory-service floor is roughly 43 requests/hour: 12 Glip marker refreshes, 12 concerned-item pulls, 12 follow-thread-hit pulls, 4 notification feed polls, about 1 health check, and up to 2 calendar syncs, plus one-time startup/profile work. The measured overnight floor of 154-178/hour is much higher because every fresh worker lifetime reruns startup profile/Glip/concerned/follow work.
- The gateway SSH route became unavailable after the 24-hour/6-hour aggregates were collected, so a separate later five-minute tail delta could not be measured. The gateway data still ended only minutes before the browser inspection, and the browser supplied a fresh provider-failure observation.

## 2026-08-12 Repair Design And Usage Analytics Answer

- The audit's exact 24-hour LLM table was queried from the deployed `${DATA_DIR}/analytics/usage.db` `usage_events` rows, not copied from the Markdown document. `docs/features/usage_analytics.md` documents the same pipeline and the supported dashboard: frontend `handleLLMRequest` emits `feature`, `model`, `status`, `errorKind`, and token fields; `/usage/report` aggregates feature failures and model totals; the dashboard supports frontend-only filtering and feature drilldown.
- The current dashboard can reproduce the important shape of the table: expand the Memory Capture capability to see `passive_webpage_memory_analysis` calls/failures, inspect the model panel for model/token totals, and compare the Message Analysis capability. It does not currently render a single cross-tab row grouping exactly `feature + model + status/errorKind`; exact `network` error classification therefore still comes from raw analytics SQL or a future report dimension.
- `.env.development` selects Dify and points the primary app to an internal HTTP host; the real browser's new passive path fails at `fetch`. The production `.env` selects a separate OpenAI-compatible OneAPI path. Provider recovery must validate the actual dev Dify endpoint with synthetic content and must not print/replay keys in logs.
- The task has extensive overlapping worktree changes. Existing diffs in `src/background.ts` and `src/contentScriptWebIntelligence.ts` belong to Jira/Roadmap/Recall work and must be preserved; new patches must be anchored narrowly around webpage caches, bootstrap scheduling, and page-candidate timers.
- Credential-safe synthetic probes confirmed the configured development Dify host fails before HTTP (`fetch failed`) over both HTTP and HTTPS. Reusing the same Dify app key against `dify.int.rclabenv.com` returns 401. The other configured Dify keys reach that host but return `not_chat_app`, because they belong to workflow apps rather than chat apps.
- Backoff alone would reduce noise but would not satisfy the requirement that webpage analysis work. The preferred recovery is a dedicated memory-service webpage-analysis route using the already deployed backend LLM provider. The page body is already sent to the same memory-service for `/source-memory/candidates/score`, so this changes execution location without adding a new data destination; it also removes this feature's dependency on a provider key embedded in the extension.
- The implemented route is `POST /api/v1/source-memory/webpage-analysis`. It owns the fixed prompt, rejects arbitrary fields and content outside 120-16,000 characters, disables provider retry for this logical call, and returns the prompt version with the raw JSON. The extension then applies the existing evidence normalizer, so unsupported facts still fail closed.
- A credential-safe real-provider call through the new service completed in 9.0 seconds with `remember`, one grounded fact, one grounded action item, and prompt version `passive-webpage-memory-v2`. This proves the configured local memory-service provider can execute the production prompt; the route itself is separately covered by build/unit tests and the extension route by browser E2E.
- Failed snapshots now retain a storage-session backoff with 5/10/15-minute cooldowns. Automatic retries are suppressed during the cooldown, while manual force bypasses the cooldown and a later success clears it. Backend final LLM failures are also recorded as zero-token usage events with low-cardinality error kinds.
- Candidate scoring now has two signals: the server-score signal changes only at copy, 90-second dwell, 60% scroll, or semantic-content transitions; the local auto-save signal additionally changes at 240/480 seconds and 75/85/90% scroll. Later auto-save decisions reuse the latest deterministic score instead of POSTing every 30 seconds.
- Storage-backed startup leases now cover stored identity sync, bootstrap Glip marker refresh, and concerned/follow remote sync. Explicit storage-change Glip refreshes and user/manual operations remain immediate; only automatic MV3 worker startup amplification is throttled.
- The deployed real-provider check produced an analytics row exactly as intended: `side=backend`, `capability=memory_capture`, `route=/source-memory/webpage-analysis`, `model=claude-sonnet-4-6`, `status=ok`, 873 prompt tokens, and 637 completion tokens. The dashboard can show the route/calls/failures/tokens under Memory Capture and the model/token total in its model panel; the exact combined row still requires raw SQL because the dashboard does not cross-tab every dimension together.
